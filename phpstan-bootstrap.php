<?php

declare(strict_types=1);

// Registers package Blueprint macros so Larastan can resolve them via the
// $macros static property. The macros themselves are registered by the
// service provider; this file mirrors the registration for static analysis.
// Populated as the macro surface lands in later phases.
